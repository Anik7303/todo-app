import "./Header.scss";

const Header = (props) => {
    return (
        <header className="section-header">
            <h1 className="heading-1">Todo App</h1>
        </header>
    );
};

export default Header;
